# Enter either the ngrok hostname or full URL. Do not include an HTML page path.
VITE_ALLOWED_HOST=https://your-ngrok-domain.ngrok-free.app

# Vite proxies /api to the Python service at http://127.0.0.1:8000.
VITE_API_URL=/api
